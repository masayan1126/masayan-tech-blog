---
import Link from "@/components/Link/Link.astro";
import { ARTICLES_PATH } from "@/features/Blog/List/constants/path";
import BaseLayout from "@/layouts/BaseLayout.astro";
import { Image } from "astro:assets";
import profileImage from "@/assets/images/bird-pc.webp";
// テーマカラーはCSS変数（--color-primary, --color-secondary）経由で使用
---

<BaseLayout
  title={"404 - ページが見つかりません"}
  description={"404 - ページが見つかりません"}
  context={"404"}
  breadCrumbsList={[]}
>
  <div class="min-h-screen flex items-center justify-center px-4 py-16">
    <div class="max-w-4xl w-full text-center">
      <!-- インコの画像 -->
      <div class="mb-8 animate-bounce-gentle">
        <Image
          src={profileImage}
          width={192}
          height={192}
          alt="迷子のインコ"
          format="avif"
          class="w-32 h-32 md:w-48 md:h-48 mx-auto rounded-full"
        />
      </div>

      <!-- 404 大きな数字 -->
      <div class="relative mb-8">
        <h1 class="text-9xl md:text-[12rem] font-bold select-none" style="color: var(--color-primary);">
          404
        </h1>
        <div class="absolute inset-0 text-9xl md:text-[12rem] font-bold blur-2xl animate-pulse" style="color: rgba(var(--color-primary-rgb), 0.2);">
          404
        </div>
      </div>

      <!-- メッセージ -->
      <div class="space-y-4 mb-12 animate-fade-in-up">
        <h2 class="text-3xl md:text-4xl font-bold text-white">
          ページが見つかりません
        </h2>
        <p class="text-lg md:text-xl text-gray-300 max-w-2xl mx-auto">
          お探しのページは移動または削除された可能性があります。<br />
          以下のリンクから目的のページをお探しください。
        </p>
      </div>

      <!-- ナビゲーションカード -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12 animate-fade-in-up" style="animation-delay: 0.2s;">
        <!-- ホームへ -->
        <a
          href="/page/1"
          class="group relative p-6 rounded-2xl transition-all duration-300 hover:scale-105 card-primary"
        >
          <div class="text-4xl mb-3 group-hover:scale-110 transition-transform duration-300">
            🏠
          </div>
          <h3 class="text-xl font-bold text-white mb-2">ホーム</h3>
          <p class="text-sm text-gray-400">トップページに戻る</p>
        </a>

        <!-- ブログ一覧へ -->
        <a
          href={ARTICLES_PATH}
          class="group relative p-6 rounded-2xl transition-all duration-300 hover:scale-105 card-secondary"
        >
          <div class="text-4xl mb-3 group-hover:scale-110 transition-transform duration-300">
            📝
          </div>
          <h3 class="text-xl font-bold text-white mb-2">ブログ記事</h3>
          <p class="text-sm text-gray-400">記事一覧を見る</p>
        </a>

        <!-- カテゴリ一覧へ -->
        <a
          href="/categories"
          class="group relative p-6 rounded-2xl transition-all duration-300 hover:scale-105 card-mix"
        >
          <div class="text-4xl mb-3 group-hover:scale-110 transition-transform duration-300">
            🏷️
          </div>
          <h3 class="text-xl font-bold text-white mb-2">カテゴリ</h3>
          <p class="text-sm text-gray-400">カテゴリから探す</p>
        </a>
      </div>

      <!-- 検索バー風のリンク -->
      <div class="animate-fade-in-up" style="animation-delay: 0.4s;">
        <a
          href={ARTICLES_PATH}
          class="inline-flex items-center gap-3 px-8 py-4 rounded-full transition-all duration-300 group search-button"
        >
          <span class="text-2xl group-hover:scale-110 transition-transform duration-300">🔍</span>
          <span class="text-white">記事を検索する</span>
        </a>
      </div>

      <!-- 装飾的な要素 -->
      <div class="absolute top-20 left-10 w-20 h-20 rounded-full blur-3xl animate-float decoration-primary"></div>
      <div class="absolute bottom-20 right-10 w-32 h-32 rounded-full blur-3xl animate-float-delayed decoration-secondary"></div>
    </div>
  </div>
</BaseLayout>

<style>
  /* カードスタイル - プライマリカラー */
  .card-primary {
    background: rgba(var(--color-primary-rgb), 0.1);
    border: 1px solid rgba(var(--color-primary-rgb), 0.2);
  }
  
  .card-primary:hover {
    background: rgba(var(--color-primary-rgb), 0.15);
    border-color: rgba(var(--color-primary-rgb), 0.5);
    box-shadow: 0 25px 50px -12px rgba(var(--color-primary-rgb), 0.3);
  }
  
  /* カードスタイル - セカンダリカラー */
  .card-secondary {
    background: rgba(var(--color-secondary-rgb), 0.1);
    border: 1px solid rgba(var(--color-secondary-rgb), 0.2);
  }
  
  .card-secondary:hover {
    background: rgba(var(--color-secondary-rgb), 0.15);
    border-color: rgba(var(--color-secondary-rgb), 0.5);
    box-shadow: 0 25px 50px -12px rgba(var(--color-secondary-rgb), 0.3);
  }
  
  /* カードスタイル - ミックス */
  .card-mix {
    background: rgba(var(--color-primary-rgb), 0.1);
    border: 1px solid rgba(var(--color-primary-rgb), 0.2);
  }
  
  .card-mix:hover {
    background: rgba(var(--color-primary-rgb), 0.15);
    border-color: rgba(var(--color-primary-rgb), 0.5);
    box-shadow: 0 25px 50px -12px rgba(var(--color-primary-rgb), 0.3);
  }
  
  /* 検索ボタン */
  .search-button {
    background: rgba(var(--color-primary-rgb), 0.1);
    border: 1px solid rgba(var(--color-primary-rgb), 0.2);
  }
  
  .search-button:hover {
    background: rgba(var(--color-primary-rgb), 0.2);
    border-color: rgba(var(--color-primary-rgb), 0.4);
  }
  
  /* 装飾要素 */
  .decoration-primary {
    background-color: rgba(var(--color-primary-rgb), 0.2);
  }
  
  .decoration-secondary {
    background-color: rgba(var(--color-secondary-rgb), 0.2);
  }

  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: fade-in-up 0.6s ease-out forwards;
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  .animate-float {
    animation: float 4s ease-in-out infinite;
  }

  .animate-float-delayed {
    animation: float 4s ease-in-out infinite;
    animation-delay: 2s;
  }

  @keyframes bounce-gentle {
    0%, 100% {
      transform: translateY(0px) rotate(-3deg);
    }
    50% {
      transform: translateY(-10px) rotate(3deg);
    }
  }

  .animate-bounce-gentle {
    animation: bounce-gentle 2s ease-in-out infinite;
  }
</style>
